<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.MainActivity">

    <LinearLayout
        android:orientation="vertical"
        android:layout_height="match_parent"
        android:layout_width="match_parent">
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/appbar"
            android:theme="@style/Theme.Taskapp.AppBarOverlay">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                app:popupTheme="@style/Theme.Taskapp.PopupOverlay" />

        </com.google.android.material.appbar.AppBarLayout>

        <ViewFlipper
            android:id="@+id/vf"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <include android:id="@+id/loading" layout="@layout/activity_optimizing" />
            <include android:id="@+id/recycler_layout" layout="@layout/content_main" />
            <include android:id="@+id/no_entries" layout="@layout/activity_no_entries" />

        </ViewFlipper>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/addEventLayout"
        android:orientation="horizontal"
        android:layout_gravity="bottom|end"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_margin="@dimen/standard_21"
        android:layout_height="wrap_content">
        <TextView
            android:id="@+id/addEventLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/item_name"
            android:layout_marginEnd="5dp"
            android:alpha="0"
            android:textColor="@color/text_primary"
            android:text="@string/add_event"/>
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/addEventFab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:fabSize="mini"
            app:srcCompat="@drawable/baseline_edit_calendar_21" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/addTaskLayout"
        android:orientation="horizontal"
        android:layout_gravity="bottom|end"
        android:gravity="center"
        android:layout_margin="@dimen/standard_21"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <TextView
            android:id="@+id/addTaskLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/item_name"
            android:layout_marginEnd="5dp"
            android:alpha="0"
            android:textColor="@color/text_primary"
            android:text="@string/add_task"/>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/addTaskFab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:fabSize="mini"
            app:srcCompat="@drawable/ic_baseline_add_24" />
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="@dimen/fab_margin"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/plus"
        app:layout_behavior="com.google.android.material.behavior.HideBottomViewOnScrollBehavior"
        app:srcCompat="@drawable/ic_baseline_add_24"
        tools:ignore="ImageContrastCheck" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>